# My-Girok Identity Service Helm Values Example
#
# USAGE:
#   1. Copy this file: cp values.yaml.example values.yaml
#   2. Edit values.yaml with your settings
#   3. Install: helm install my-girok-identity . -f values.yaml
#
# IMPORTANT:
#   - values.yaml is gitignored - never commit your actual configuration
#   - Customize for your environment (development, staging, production)
#   - Use External Secrets Operator for sensitive data in production

# Number of replicas
# Development: 1, Staging: 2, Production: 3+
replicaCount: 2

image:
  repository: ghcr.io/beegy-labs/my-girok/identity-service
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion
  # Development: "dev", Staging: "staging", Production: "latest" or version tag
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 1000
  runAsNonRoot: true
  runAsUser: 1000

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: true

service:
  type: ClusterIP
  port: 3000
  targetPort: 3000

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rate limiting
    nginx.ingress.kubernetes.io/limit-rps: "100"
  hosts:
    # CHANGE THIS: Update with your domain
    - host: identity-api.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: identity-api-tls
      hosts:
        - identity-api.example.com

# Resource limits
# Development: Lower limits, Staging: Medium, Production: Higher
resources:
  limits:
    cpu: 1000m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

# Auto-scaling
# Development: Disable, Staging/Production: Enable
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - identity-service
          topologyKey: kubernetes.io/hostname

# ============================================================================
# Database Migration (goose)
# ============================================================================

migration:
  enabled: true  # Enable ArgoCD PreSync migration hook

# ============================================================================
# Application Configuration
# ============================================================================

app:
  # Environment: development, staging, production
  nodeEnv: production
  port: 3000

  # Database Configuration (Single Database Architecture)
  # CHANGE THESE VALUES
  database:
    host: your-database-host.example.com
    port: 5432
    name: identity_dev
    username: identity_dev
    # Password should be in External Secret

  # JWT Configuration
  jwt:
    accessExpiration: "15m"
    refreshExpiration: "14d"

  # Frontend URL
  # CHANGE THIS
  frontendUrl: "https://example.com"

  # Auth Service URL (for backward compatibility)
  authServiceUrl: "http://auth-service:3001"

# ============================================================================
# Valkey (Redis-compatible) Configuration
# ============================================================================
valkey:
  enabled: true
  host: "valkey.example.com"
  port: 6379
  db: 0
  secretName: "identity-service-valkey"
  secretKey: "password"

# ============================================================================
# Kafka/Redpanda Configuration (for Event Sourcing)
# ============================================================================
kafka:
  enabled: true
  brokers: "redpanda.example.com:9092"
  # Topics are auto-created by the service

# ============================================================================
# External Secrets Configuration
# ============================================================================
externalSecrets:
  enabled: true
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore

# Health checks
livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

# ============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATIONS
# ============================================================================
#
# DEVELOPMENT:
#   replicaCount: 1
#   image.tag: "dev"
#   autoscaling.enabled: false
#   app.nodeEnv: development
#   app.database.name: identity_dev
#   resources.limits.cpu: 500m
#   resources.limits.memory: 256Mi
#   ingress.hosts[0].host: identity-api-dev.example.com
#
# STAGING:
#   replicaCount: 2
#   image.tag: "staging"
#   autoscaling.enabled: true
#   autoscaling.minReplicas: 2
#   autoscaling.maxReplicas: 5
#   app.nodeEnv: staging
#   resources.limits.cpu: 750m
#   resources.limits.memory: 384Mi
#   ingress.hosts[0].host: identity-api-staging.example.com
#
# PRODUCTION:
#   replicaCount: 3
#   image.tag: "latest" or "v1.0.0"
#   autoscaling.enabled: true
#   autoscaling.minReplicas: 3
#   autoscaling.maxReplicas: 10
#   app.nodeEnv: production
#   resources.limits.cpu: 1000m
#   resources.limits.memory: 512Mi
#   ingress.hosts[0].host: identity-api.example.com
#   affinity.podAntiAffinity: requiredDuringSchedulingIgnoredDuringExecution
#
# ============================================================================
