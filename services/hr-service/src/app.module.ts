/**
 * HR Service - App Module
 *
 * STATUS: Structure Backup Only (Not Implemented)
 *
 * Future modules to be imported:
 * - AttendanceModule
 * - LeaveModule
 * - DelegationModule
 * - EmployeeModule
 */

import { Module } from '@nestjs/common';

@Module({
  imports: [
    // Future imports:
    // AttendanceModule,
    // LeaveModule,
    // DelegationModule,
    // EmployeeModule,
  ],
  controllers: [],
  providers: [],
})
export class AppModule {}
