apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-main.fullname" . }}
  labels:
    {{- include "web-main.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-main.selectorLabels" . | nindent 4 }}
