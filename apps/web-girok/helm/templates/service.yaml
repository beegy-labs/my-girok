apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-girok.fullname" . }}
  labels:
    {{- include "web-girok.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-girok.selectorLabels" . | nindent 4 }}
