# 문서 (한국어)

> 이 디렉터리는 CI 번역으로 자동 생성됩니다.
> 수동 편집 금지 - 모든 변경은 `docs/en/`에서 이루어져야 합니다.

## 번역 상태

- **소스**: `docs/en/`
- **상태**: 번역 대기 중
- **마지막 동기화**: -

## 디렉터리 구조

```
docs/kr/
├── README.md           # 이 파일 (자동 생성)
├── policies/           # 정책 문서 (번역)
├── services/           # 서비스 문서 (번역)
├── guides/             # 가이드 문서 (번역)
└── ...
```

## 참고

- 영어 원문: [`docs/en/`](../en/)
- 번역 설정: [`docs/llm/_meta/i18n-config.yaml`](../llm/_meta/i18n-config.yaml)
