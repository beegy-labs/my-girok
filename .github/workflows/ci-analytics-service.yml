name: CI - Analytics Service

on:
  push:
    branches: [develop, release, main]
    paths:
      - 'services/analytics-service/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
  pull_request:
    branches: [develop, release, main]
    paths:
      - 'services/analytics-service/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'Deploy environment'
        required: false
        default: 'develop'
        type: choice
        options:
          - develop
          - release
          - main

jobs:
  ci:
    uses: ./.github/workflows/_service-ci.yml
    with:
      service_name: analytics-service
      service_path: services/analytics-service
      image_name: beegy-labs/analytics-service
      gitops_file_prefix: platform-analytics-service
      needs_prisma: false
      pnpm_filter: '@my-girok/analytics-service'
    secrets:
      GITEA_USERNAME: ${{ secrets.GITEA_USERNAME }}
      GITEA_TOKEN: ${{ secrets.GITEA_TOKEN }}
      GITOPS_PAT: ${{ secrets.GITOPS_PAT }}
