name: CI - Auth Service

on:
  push:
    branches: [develop, release, main]
    paths:
      - 'services/auth-service/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
  pull_request:
    branches: [develop, release, main]
    paths:
      - 'services/auth-service/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'Deploy environment'
        required: false
        default: 'develop'
        type: choice
        options:
          - develop
          - release
          - main

env:
  REGISTRY: gitea.girok.dev
  IMAGE_NAME: beegy-labs/auth-service

jobs:
  check:
    name: Check
    runs-on: [self-hosted, kubernetes, gitops]
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '24' }
      - uses: pnpm/action-setup@v4
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/share/pnpm/store
            node_modules
            services/auth-service/node_modules
            packages/types/node_modules
            packages/nest-common/node_modules
          key: ${{ runner.os }}-pnpm-auth-${{ hashFiles('**/pnpm-lock.yaml') }}
      - run: pnpm install --frozen-lockfile --prefer-offline
      - run: pnpm --filter @my-girok/types build
      - run: pnpm --filter @my-girok/auth-service prisma:generate
      - run: pnpm --filter @my-girok/nest-common build
      - run: cd services/auth-service && pnpm test && pnpm lint

  build:
    name: Build
    needs: check
    runs-on: [self-hosted, kubernetes, gitops]
    timeout-minutes: 10
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.GITEA_USERNAME }}
          password: ${{ secrets.GITEA_TOKEN }}
      - name: Meta
        id: meta
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          DEPLOY_ENV="${{ inputs.deploy_env || 'develop' }}"
          case "${DEPLOY_ENV}" in
            main) TAG="${SHORT_SHA}"; TAGS="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${TAG},${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" ;;
            develop) TAG="develop-${SHORT_SHA}"; TAGS="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${TAG},${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:develop" ;;
            release) TAG="release-${SHORT_SHA}"; TAGS="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${TAG},${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:release" ;;
            *) TAG="develop-${SHORT_SHA}"; TAGS="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${TAG}" ;;
          esac
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "tags=${TAGS}" >> $GITHUB_OUTPUT
          echo "deploy_env=${DEPLOY_ENV}" >> $GITHUB_OUTPUT
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: ./services/auth-service/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

  deploy:
    name: Deploy
    needs: build
    runs-on: [self-hosted, kubernetes, gitops]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          repository: beegy-labs/platform-gitops
          token: ${{ secrets.GITOPS_PAT }}
          path: gitops
      - name: Update & Push
        run: |
          cd gitops && git pull --rebase
          DEPLOY_ENV="${{ inputs.deploy_env || 'develop' }}"
          case "${DEPLOY_ENV}" in
            main) FILE="clusters/home/values/platform-auth-service-prod.yaml"; ENV="production" ;;
            develop) FILE="clusters/home/values/platform-auth-service-dev.yaml"; ENV="development" ;;
            release) FILE="clusters/home/values/platform-auth-service-staging.yaml"; ENV="staging" ;;
            *) FILE="clusters/home/values/platform-auth-service-dev.yaml"; ENV="development" ;;
          esac
          sed -i "s|tag: \".*\"|tag: \"${{ needs.build.outputs.tag }}\"|g" ${FILE}
          git config user.name "GitHub Actions Bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ${FILE}
          git diff --staged --quiet || git commit -m "chore(${ENV}): update auth-service to ${{ needs.build.outputs.tag }}"
          for i in 1 2 3; do git push && break || { sleep 3; git pull --rebase; }; done
      - name: Cleanup
        env: { GITEA_TOKEN: "${{ secrets.GITEA_TOKEN }}" }
        run: |
          DEPLOY_ENV="${{ inputs.deploy_env || 'develop' }}"
          case "${DEPLOY_ENV}" in
            main) P="" ;;
            develop) P="develop-" ;;
            release) P="release-" ;;
            *) P="develop-" ;;
          esac
          curl -s -H "Authorization: token ${GITEA_TOKEN}" "https://gitea.girok.dev/api/v1/packages/beegy-labs/container/auth-service" | \
            jq -r ".[] | select(.version | startswith(\"${P}\")) | \"\(.created_at)|\(.version)\"" | sort -r | tail -n +11 | \
            while IFS='|' read -r _ v; do curl -sX DELETE -H "Authorization: token ${GITEA_TOKEN}" "https://gitea.girok.dev/api/v1/packages/beegy-labs/container/auth-service/${v}"; done
